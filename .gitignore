# Environnement de développement local
.claude/
.env

# Variables d'environnement
.env
.env.local
.env.*.local

# Logs
*.log
error_log

# Éditeurs / IDE
.idea/
.vscode/
*.swp
*.swo
*~
.editorconfig

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Node (si outils de build front ajoutés ultérieurement)
node_modules/
npm-debug.log*
yarn-error.log*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.cache/
.playwright/

# Composer (outillage de développement uniquement : phpstan. Aucune
# dépendance au runtime — cf. composer.json)
vendor/

# MkDocs (documentation administrateur, cf. docs-plan/PLAN.md) : site/ est
# reconstruit par la CI (.github/workflows/docs.yml) à chaque déploiement,
# jamais versionné.
site/

# WordPress
wp-cli.local.yml

# Artefacts vidéo/planche-contact générés localement (non versionnés)
/demo-*.mp4
/planche*.png

# Livrables et documents de travail hors dépôt
claude-export.zip
claude-export/
rapport-audit-*.html
/*.zip
theme/Archive/Archive.zip

# Cache local de l'outillage de design (Impeccable) : par session/poste,
# jamais un artefact du dépôt. .impeccable/design.json reste versionné.
.impeccable/hook.cache.json
.impeccable/config.local.json
