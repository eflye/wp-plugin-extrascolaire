# Copier ce fichier en .env et ajuster les valeurs.
# .env n'est pas commité (voir .gitignore).

# Activer Mailpit pour capturer les e-mails en local.
# Démarrer avec : MAILPIT_ENABLED=true podman compose --profile mailpit up -d
MAILPIT_ENABLED=false
